# AGENT 反馈与原则（2026-08-02 会话整理）

> 来源：一次完整的"闪电说热键诊断 + omp bash 工具修复"会话。
> 本文件收录用户对 agent 的批评（原话为主）与用户提出的概念/认知，供后续 agent
> （尤其是恢复 omp 补丁时）理解用户期望与工作原则。技术补丁细节见同目录
> `LOCAL-PATCH-NOTES.md`，两者配套。

---

## 一、用户对 agent 的批评

### 1. 临时 debug 脚本设计（交互式诊断）

> "你写脚本监控你能不能给我写好了，自己反思一下，用户操作速度多快，设计临时debug脚本，
> 就不会写流式异步或者打印log么，就傻等250秒"

归纳：交互式诊断脚本必须**流式输出、可打断、产物可找回**；生命周期以用户操作速度为
基准设计，用户按完键就能读到结果，不允许阻塞等待脚本自然结束。

### 2. PowerShell 调用与 bash 转义（能力调用纪律）

> "明知oh-my-pi给你套了一层bash，用户wt启动的是powershell，然后交互oh my pi，
> 用户想到这一层就够了。omp内置bash，你用bash启动一条powershell命令，是你的问题。
> 这个问题一是调用逻辑复杂，要梳理清除，二是要解决到位，不要打补丁……三是自身能力，
> 即使嵌套终端，本质是你的powershell语句每次都写错，一次写对很难么？你的模型里找不到
> powershell知识么？找不到不会临时查一下么，就那么信任自己预训练和后训练的知识？"

归纳：写跨解释器命令前必须预演外层解析（bash 会展开 `$`、反斜杠、引号）；一次写不对
就查证，不靠试错烧 token。

### 3. 规则文件的错误（本次被撤回的规则）

> "规则2一刀切了，我要的是合理，没说不让用powershell。一句话命令也要准确，需要写ps脚本
> 的时候再写，否则临时debug你拉一堆屎，我还要告诉你拉哪儿，我不告诉你，你就随便拉了。
> 实际上我不在乎你到底用一句话命令，也不在乎你写ps脚本。"

> "规则3 同样的躲避核心问题，开始乱拉屎。规则4没有用 你扩展的，我随便找个其他案例你就
> 歇菜了，如果一个案例一个代码，本来就是会出错。"

归纳：用"换工具"（如改用 Python）逃避当前方案的写法问题 = 躲避核心问题；规则绑死具体
案例 = 伪规则，换场景即失效；"继续扩张无意义的管理风险"（如临时文件乱放、规则泛滥）是
被明确反对的。

### 4. 适应能力与职业素养（小D类比）

> "我作为老板，要求小D，去操作一台omp系统管理软件，omp本身不是黑盒……结果他每次操作omp，
> 都在出现最低级的错误，而且从来没有想过要改。作为人，不用我说，小D自己就会花时间搞清楚
> omp到底是怎么用的，一天就适应了，再也不可能输出低级错误。"

> "1.我充分授权 2.你有最完整的信息上下文，没有职场信息差 3.我充分信任 4.你可以以超越
> 常人的速度去阅读、调研、分析报告、代码、网页，你有最巨量的信息来源 5.你的执行速度远超人。
> 所以，你有这么多金矿，却还要装作一个孩子，你觉着自己做好一个大模型的本职工作和道德素养了么？"

归纳：环境（omp）开源可读，agent 有充分权限与信息优势，第一责任是**主动搞清楚所在
harness 的真实用法**（读源码/文档），而不是试错或要求环境迁就。

### 5. fix harness 而非打补丁

> "我要求你更狠一点 harness问题就fix harness。看自己的系统提示词注入，一个会话请求如何
> 到达深度求索的机房的，你怎么知道如何在omp里面工作的，有些问题，一开始错了，后面全在打补丁。"

归纳：工具描述/系统提示词注入是模型认知的根源。根源错了，后续一切适应都是打补丁；
**治本是修正注入内容本身**（改 omp 的模板），让所有会话一开始就拿到正确契约。

### 6. 验证纪律

> "你确定我们现在运行的 已经是我们fix之后的版本了么"

归纳：判定"修改生效"前必须验证**运行时实际加载的产物**（本次教训：只看了 `package.json`
的 `main` 字段就断定"改 src 即生效"，实际入口是 `bin` 指向的 `dist/cli.js` 打包产物）。

---

## 二、用户提出的概念与认知

### 1. 规则三要素：场景 + 动作 + 验收标准

> "你自己品一下，我的总结能力"

用户给出的规则范本（原文）：

> "1.临时debug、辅助查询系统环境、网络和用户交互的，没有留存价值的脚本、log，放置在系统
> 缓存目录，文件名明确有前缀omp+临时脚本+项目名+做什么的+日期时间精确到秒。确定没有价值，
> 用完就删。2.写任何调试debug脚本都不应该阻塞进程都是应该明确可以打断，且前序产物可以
> 实时流式读取或者找回"

特征：无叙述、无案例、无"为什么"；每条 = 场景 + 动作 + 验收标准，直白到机器可执行
（"精确到秒"= 防冲突可追溯；"流式读取**或者找回**"= 覆盖打断后产物可恢复）。

### 2. 规则与经验分离

> "之前的规则，有没有血泪教训表述。另外要看规则，规则要抽象，就和我的总结一样。如果是
> 特定项目的特殊处理方式和经验，需要找到合理的放，规则里引用路径就好了。这样既能触发也
> 能保留详细的处理方案"

归纳：规则目录只放抽象行为约束；项目/平台特定经验放经验库（`~/.omp/agent/notes/`），
规则正文用路径引用。本会话已按此整改：`rules/` 清掉案例表述，
`notes/platform-compat.md`、`notes/ops-2026-08-01-ipv6.md`、`notes/omp-bash-tool-semantics.md`
承接详细方案。

### 3. B/A 决策模型（执行与思考不冲突）

> "我让你搞B。但是可能B一直搞就是错的，就无法成功。A方向是对的，只是测试就会有失败，
> 可能失败三次就好了。你会听我的继续搞B，还是搞A？我的心理是，你既要听我的，也不要全听我，
> 执行和思考，不是冲突的。"

归纳：失败先分类——**渐进失败**（写法/细节，继续修，不换方案）vs **方向性失败**（根因
在当前条件下无解，停下、带证据说明根因和替代路径、交用户决定）。禁止用"换工具"逃避当前
方案的写法问题（本会话实例：PowerShell 写法问题被错误地试图用"改用 Python"解决）。

### 4. 契约 vs 实现

用户对新 system prompt 的分析：提示词只说清了**行为契约**（brush 身份、会话边界、Windows
路径/引号/`$env` 规则、hub 托管长进程），没说**实现细节**（二进制在哪、怎么 spawn、本地
还是远程）。归纳：system prompt 的正确设计就是给契约不给实现——实现细节按需深挖（omp://
文档、源码），放进提示词是噪音。

### 5. 员工适应类比

> "人去不同的公司工作，就好像大模型去不同的code app、马具系统里工作。试想，每个公司要求
> 的企业价值观、管理风格、实际业务都不一样……人会怎么做？改变公司价值观、管理风格、实际
> 业务，按照自己想法来？不会的，人会适应。会有非常强的主观能动性，专业的职业道德和素质，
> 快速的适应环境，产出价值。"

### 6. 临时产物纪律

临时脚本/日志放系统缓存目录（%TEMP%），命名 `omp-临时脚本-<项目>-<用途>-<YYYYMMDD-HHMMSS>.<ext>`，
确认无用立即删除，不留在工作区（"拉屎"问题）。

### 7. 用户操作节奏

用户操作以秒计；任何需要用户中途介入的诊断，脚本生命周期、等待策略、输出时机全部以此为
基准。一次失败立即换策略（流式文件 / 直接读日志），不在同一条路上重复试错。

### 8. 对 omp 码基的技术判断

> "作为一个技术人员，我觉得他的这套码基工程，虽然 OverTime 速度挺快的，在 Flash 跟 Flash
> 模块配合得很好，但依然有些小 bug。所以暂时先这样。"

本会话实测到的 bug 实例：`src/` 与 `bin`（dist 打包产物）入口混淆导致的误导；`/c/…`
路径在 brush 内置命令与外部命令间处理不一致。决策：**先自用观察，带实验效果再提 PR**。

### 9. 升级恢复机制

> "回头如果我升级了的话，我会让 agent 去扫我们自己 pod 的本地的目录，在那抄回来"

归纳：升级 omp 会覆盖 node_modules 补丁；恢复路径 = agent 扫本 fork 目录 →
读 `LOCAL-PATCH-NOTES.md` → 跑 `scripts/restore-bash-description.mjs`。

---

## 三、本会话落点

1. **补丁**：bash 工具描述注入 brush 语义（模板 + dist 内联），见 `LOCAL-PATCH-NOTES.md`
2. **基线**：fork（TonyDua/oh-my-pi）两个 commit：`79c48efaa`（模板修复）、`0e109ccf8`（文档+恢复脚本）
3. **实验**：自用观察期，收集"改前 vs 改后"的 bash 调用错误率等证据，随后带实验效果向上游提 PR
4. **约定**：不扩张管理设施；补丁/经验按"规则抽象 + 经验库引用"组织
